/*
NOTE:
Queues are first in, first out data structure type. 
Functions common in queues - enqueue(push), dequeue(shift), front, size, isEmpty.

PriorityQueues - in priority queues you pass the priority of the element - elements with higher priority are sent to the beginning of the array. 
 */
